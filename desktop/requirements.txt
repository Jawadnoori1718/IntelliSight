# Big Brother Desktop — dependencies
#
# A focused, offline, real-time object detector. No API keys, no cloud.

# GUI toolkit (lean; QtCore/QtGui/QtWidgets — no heavy addons)
PySide6-Essentials>=6.7

# Webcam capture
opencv-python>=4.9

# Open-vocabulary object detection — YOLO-World via Ultralytics (pulls in
# torch/torchvision), runs on the Apple GPU (MPS) when available. The model and
# its small text helper (`clip`) auto-download on first run.
ultralytics>=8.3.0
