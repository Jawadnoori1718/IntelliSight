# IntelliSight backend dependencies

# ── Web server (Phase 2) ──
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
python-multipart>=0.0.9        # file uploads for /api/detect

# ── Object detection (Phase 5) ──
# ultralytics pulls in torch, torchvision, opencv-python, numpy and pillow.
ultralytics>=8.3.0
